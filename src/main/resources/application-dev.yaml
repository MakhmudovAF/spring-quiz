spring:
  main:
    log-startup-info: true # логирование деталей старта разрешено
    banner-mode: off

logging.level:
  root: TRACE
  ru.yandex.practicum.quiz.QuizApp: WARN

spring-quiz:
  report:
    mode: "concise"